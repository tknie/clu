# Configureation file
rest-server:
  version: v2
  configWatcher: true
  statisticTimer: true
server:
  location:
    tracelocation: ${SERVER_HOME}/logs/trace.log
    serverlocation: ${SERVER_HOME}/logs/server.log
    loglevel: info
    maxsize: 0
    maxbackups: 0
    maxage: 0
    compress: true
  service:
    - port: 8030
      type: http
    - port: 8033
      type: https
      certificate: ${SERVER_HOME}/keys/certificate.pem
      key: ${SERVER_HOME}/keys/key.pem
  JWT:
    issuer: example.com
    expire: 14400
    encrypt: true
    publicKey: ${SERVER_HOME}/keys/apiKey.pem
    privateKey: ${SERVER_HOME}/keys/apiKey.prv
  login:
    authenticationServer:
      - type: file
        passwordFile: ${SERVER_HOME}/configuration/realm.properties
      - type: system
  content: /aaa
  plugins: plugins
  shutdown:
    passcode: {}
    # Password to shutdown, will be send encrypted
database:
  modelling:
    Modeling:
      - Database: DBname
        SourceTable: xxx
        SourceField: ""
        DestinationTable: ""
        DestinationField: ""
  access:
    databases:
      - driver: postgres
        target: postgres://${POSTGRES_USER}:${POSTGRES_PASS}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DATABASE}
        user: ${POSTGRES_USER}
        password: ${POSTGRES_PASS}
#     - driver: mysql
#       url: admin:Testtkn1+@tcp(lion.fritz.box:3306)/Bitgarten
#       table: xxx
tasks:
  use_role: true
  directory: ${SERVER_HOME}/log
  database:
    table: Jobs
fileTransfer:
  Admin:
    role: xxx
  directories:
    directory:
      - name: abc
        location: ${DATADIR}
